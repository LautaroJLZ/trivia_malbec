import { useEffect } from "react";
import { NavLink } from "react-router-dom";

const Inicio = () => {
    useEffect(() => {
        window.scrollTo(0, 0);
    }, []);
    return (
        <div>
            
        </div>
    );
};

export { Inicio };