import { useEffect } from "react";

const Inicio = () => {
    useEffect(() => {
        window.scrollTo(0, 0);
    }, []);
    return (
        <div>
        </div>
    );
};

export default Inicio ;